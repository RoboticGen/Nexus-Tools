/**
 * ESP32 Uploader Styles Entry Point
 *
 * Only the package's own stylesheet now — `antd-overrides.css` went away with
 * the antd components it was overriding.
 */

import './esp32.css';

export {};
