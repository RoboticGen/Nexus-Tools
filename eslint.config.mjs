import base from "@nexus-tools/eslint-config/base";

/**
 * Root config. Each app and package carries its own `eslint.config.js`; this
 * one covers loose files at the repo root only.
 */
export default base;
