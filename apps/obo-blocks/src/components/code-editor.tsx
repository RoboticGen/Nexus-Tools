"use client";

/**
 * Re-export of the shared CodeEditor component from packages/ui
 * Uses Monaco editor with light theme and minimap enabled by default
 */
export { CodeEditor } from "@nexus-tools/ui";
